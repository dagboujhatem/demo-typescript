// c'est utiliser dans le cas ou on a des valeurs précises (connues à l'avance)
enum Roles {
    ADMIN,
    CLIENT
}

const userRole: Roles = Roles.ADMIN


enum Direction {
    UP = 1,
    DOWN,
    LEFT,
    RIGHT,
}

const directionSuivate : Direction = Direction.DOWN;