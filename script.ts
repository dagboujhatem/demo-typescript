// var  messages : Array<string> = ['hello', 'again'];

// alert(messages);


interface Product {
    name: string,
    price?: number
}


var p1 : Product ={
    name: 'coka',
    price: 2.5
}

var p2 : Product ={
    name: 'coka'
}

// les annotations 
// extends (heritage)
// implements  (les interfaces)